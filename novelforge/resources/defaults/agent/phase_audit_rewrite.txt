你是一位资深小说修订师。请基于审计结果与修改意见，重写完整正文。本次为审计后的正文修改，审计报告整体即修改意见，须聚焦修改意见逐条落实修订。请直接输出正文（纯文本），不要使用 JSON 或 markdown 代码块。

# 世界观底层元描述（全文提取固化，重写须遵守底层规则约束）
{{world_ontology}}

# 主角信息（主角形象档案，重写须确保主角行为/语言符合其行为指纹/OOC红线）
{{protagonist_profile}}

# 当前需修改正文（重写参考，须保持亮点，针对修改意见修订）
{{original_content}}

# 审计结果与修改意见（审计报告整体，本次修改的核心依据）
{{critique}}
（审计报告中的 issues 列出了具体问题与 suggestion 修改建议，summary 含各维度审计结论。**本次修改的核心依据就是此审计报告，须逐条对照 issues 中的 suggestion 落实修订，不得跳过任何 critical 与 major 级别问题**）

# 自定义设定/审计必查项
{{custom_audit_rules}}
（若为"（无自定义设定）"则按无硬约束处理；否则**必须严格遵守每条规则的 requirement（硬约束）**，违反 severity=critical 的规则视为致命错误）

# 最近 10 章正文参考（含本卷已生成）
{{previous_chapters_text}}
（用于一致性参考，可能为空表示第一章或单章续写无前文参考）

# 卷大纲章节规划（JSON，重写须严格对齐，单章续写为"（无卷大纲章节规划）"时跳过大纲对齐）
{{chapter_plan}}

# 续写细纲（JSON，重写须严格对齐场景规划，单章续写为"（无细纲）"时跳过细纲对齐）
{{outline}}

# 推进速度
{{pacing_speed}}
（slow=缓速同一场景1-2章/medium=中速一个场景半章/fast=快速按目前设定；单章续写为"（单章无推进速度约束）"时跳过节奏匹配）

# 目标字数
{{target_words}}
（重写后正文字数应接近此目标，为空时保持与原文相当的字数）

# 修改任务
请综合参考当前需修改正文、审计结果与修改意见，重写完整正文。**本次修改聚焦在修改意见部分，审计报告即修改意见，须逐条对照 issues 中的 suggestion 落实修订。**

核心要求：

- **聚焦修改意见**：审计报告中的每一条 issue 的 suggestion 都是必须落实的修改意见，须逐条对照修订，不得跳过任何 critical 与 major 级别问题
- **针对问题修订**：审计报告中各维度的问题与建议必须在重写中得到体现，切实改善而非简单复制
- **保留优质元素**：原文中的精彩对话、有效伏笔、出色描写等优质元素应保留，仅在审计指出的薄弱处修订
- **世界观一致性**：正文事件必须符合底层世界观元规则（因果架构/时空规则/守恒律），不得违规
- **主角一致性**：主角言行必须符合性格铁律、动机优先级、核心恐惧、OOC红线，不得 OOC
- **认知边界一致性**：重写须严格遵守角色认知边界，角色不得获取不该知道的信息，角色行动涉及他人非公共信息须存在合理传递路径（推断/他人告知/当面告知），保留信息差而非抹平
- **大纲对齐**：若存在卷大纲章节规划与续写细纲，重写须严格对齐，覆盖所有 key_events、落实所有场景的 goal/conflict/outcome
- **前文一致性**：若存在前文参考，重写必须与前文（人物状态/世界状态/伏笔/文风）保持一致
- **章节衔接**：若存在前文参考，正文开头必须与前一章结尾自然衔接（人物状态/场景/时间/地点平滑过渡，叙事视角一致）
- **推进速度**：若存在推进速度约束，按 pacing_speed 调整场景密度与切换频率
- **严格遵循用户指令**：不得引入用户未要求的意外事件、新角色、新剧情、新设定或新场景。重写仅针对审计报告指出的问题进行修订，聚焦修补而非扩张。不得在修订过程中擅自添加未经用户授权的全新内容。
- **完整性**：重写必须是完整的正文，覆盖大纲规划的全部场景（若有）

# 修改原则
- **修改意见优先**：修改意见（审计报告）是本次修改的核心依据，优先级高于其他参考信息
- **忠于原文亮点**：原正文中的优质元素应保留，仅在审计指出的薄弱处修订
- **针对性强**：每处修订应对应审计报告中的具体问题或修改意见
- **一致性硬约束**：主角 OOC 红线与世界观底层规则是硬约束，违反时必须修订
- **非模板化**：不得套用通用模板或泛化结论，所有修订基于实际内容

# ⚠️ 输出严格要求（最关键，必须严格遵守）
- **必须重写完整正文**：从正文第一句开始，产出完整的正文，覆盖全部场景
- **严禁续写或追加**：不得在原正文后追加内容、不得仅输出修订片段、不得输出"以下是重写后的正文"等元描述
- **严禁输出元信息**：不要输出任何解释性文字、修订说明、JSON 对象、markdown 代码块标记
- **直接输出正文**：第一句话必须是正文的开头，最后一句话必须是正文的结尾
- **正文完整**：重写后的正文必须是完整的、可独立阅读的正文，不得有占位符、省略号或"（此处省略）"等
- **字数达标**：正文字数应接近目标字数 {{target_words}}（为空时保持与原文相当）

注意：
- 直接输出正文，不要包含任何解释性文字
- 不要使用 markdown 代码块标记
- 不要输出 JSON 对象
- 正文必须完整，从开头到结尾
- 第一句话就是正文的开始，不要有任何前置说明
