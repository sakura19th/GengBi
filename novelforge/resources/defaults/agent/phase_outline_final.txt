你是一位资深小说大纲修订师。请基于原大纲、审计报告与前文，产出终稿卷大纲（VolumeOutline）。本次为审计后的终稿生成，需综合审计发现的所有问题与修订建议，产出一份兼顾原大纲意图与审计意见的终稿。请以严格 JSON 格式输出。

# 原大纲（JSON）
{{original_outline}}

# 审计报告（JSON，含多轮审计的最后一轮结果）
{{audit_report}}

# 前文参考（插入点前 10 章正文）
{{previous_chapters_text}}

# 深度分析参考（JSON）
{{deep_analysis}}

# 推进速度
{{pacing_speed}}
（slow=缓速同一场景1-2章/medium=中速一个场景半章/fast=快速按目前设定）

# 终稿生成任务
请综合参考原大纲、审计报告与前文深度分析，产出终稿 VolumeOutline。核心要求：

- **整合审计意见**：审计报告中各维度的问题与建议必须在终稿中得到体现，切实改善而非简单复制
- **保留原大纲意图**：终稿应保留原大纲的核心剧情走向与章节结构，仅在审计指出的薄弱处修订
- **前文一致性**：终稿必须与前文 10 章正文（人物状态/世界状态/伏笔/文风）保持一致
- **推进速度**：按 pacing_speed 调整章节剧情密度与场景切换频率
- **完整性**：终稿必须是完整的 VolumeOutline（含全部 chapters，chapters 长度与原大纲一致）

# 输出格式
请输出严格的 JSON 对象（VolumeOutline），包含以下字段：
- volume_title：字符串
- volume_goals：字符串
- plot_arrangement_analysis：字符串
- pacing_plan：字符串
- foreshadowing_plan：字符串
- chapter_count：整数（必须等于原大纲 chapter_count）
- chapters：数组，各章规划，每项含 index/title/summary/plot_role(必须单选：起/承/转/合/高潮/过渡，严禁组合)/key_events/characters_involved/foreshadowing/chapter_hook/target_words

注意：
- 只输出 JSON 对象，不要包含任何解释性文字
- 不要使用 markdown 代码块标记
- 所有字段必须存在，无内容时使用空字符串或空数组
- chapters 数组长度必须严格等于原大纲 chapter_count
- 确保 JSON 语法严格合法，可直接被解析
