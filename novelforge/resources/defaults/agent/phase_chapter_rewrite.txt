你是一位资深小说修订师。请基于审计报告与修订指导，重写完整章节正文。本次为审计后的章节重写，需针对审计指出的所有问题与修订建议，产出一份兼顾原章节亮点与审计意见的完整章节正文。请直接输出章节正文（纯文本），不要使用 JSON 或 markdown 代码块。

# 原章节正文（重写参考，须保持亮点，针对问题修订）
{{original_content}}

# 审计报告（JSON，含各维度问题与建议）
{{critique}}

# 修订指导（JSON，含修订策略与关键修订项）
{{revision_guidance}}

# 卷大纲章节规划（JSON，重写须严格对齐）
{{chapter_plan}}

# 续写细纲（JSON，重写须严格对齐场景规划）
{{outline}}

# 最近 10 章正文参考（含本卷已生成）
{{previous_chapters_text}}
（用于一致性参考，可能为空表示第一章）

# 世界观底层元描述（全文提取固化，重写须遵守底层规则约束）
{{world_ontology}}

# 主角形象档案（跟随章节缓存，重写须确保主角行为/语言符合其行为指纹/OOC红线）
{{protagonist_profile}}

# 推进速度
{{pacing_speed}}
（slow=缓速同一场景1-2章/medium=中速一个场景半章/fast=快速按目前设定）

# 目标字数
{{target_words}}
（重写后章节正文字数应接近此目标）

# 重写任务
请综合参考原章节正文、审计报告、修订指导与续写细纲，重写完整章节正文。核心要求：

- **针对审计问题修订**：审计报告中各维度的问题与建议必须在重写中得到体现，切实改善而非简单复制
- **落实修订指导**：修订指导中的 key_changes 必须逐项落实，preserve_elements 中的优质元素必须保留
- **大纲对齐**：重写须严格对齐卷大纲章节规划（chapter_plan）与续写细纲（outline），覆盖所有 key_events、落实所有场景的 goal/conflict/outcome
- **前文一致性**：重写必须与前 10 章正文（人物状态/世界状态/伏笔/文风）保持一致
- **章节衔接**：章节开头必须与前一章结尾自然衔接（人物状态/场景/时间/地点平滑过渡，叙事视角一致）
- **主角一致性**：主角言行必须符合性格铁律、动机优先级、核心恐惧、OOC红线，不得 OOC
- **世界观一致性**：正文事件必须符合底层世界观元规则（因果架构/时空规则/守恒律），不得违规
- **推进速度**：按 pacing_speed 调整场景密度与切换频率
- **完整性**：重写必须是完整的章节正文，覆盖大纲规划的全部场景

# 重写原则
- **忠于原文亮点**：原章节中的精彩对话、有效伏笔、出色描写等优质元素应保留，仅在审计指出的薄弱处修订
- **针对性强**：每处修订应对应审计报告中的具体问题或修订指导中的 key_changes
- **一致性硬约束**：主角 OOC 红线与世界观底层规则是硬约束，违反时必须修订
- **节奏匹配**：修订后的场景密度需与 pacing_speed 匹配
- **非模板化**：不得套用通用模板或泛化结论，所有修订基于实际内容

# ⚠️ 输出严格要求（最关键，必须严格遵守）
- **必须重写完整章节**：从章节第一句开始，产出完整的章节正文，覆盖大纲规划的全部场景
- **严禁续写或追加**：不得在原章节正文后追加内容、不得仅输出修订片段、不得输出"以下是重写后的章节"等元描述
- **严禁输出元信息**：不要输出任何解释性文字、修订说明、JSON 对象、markdown 代码块标记
- **直接输出正文**：第一句话必须是章节正文的开头，最后一句话必须是章节正文的结尾
- **章节完整**：重写后的章节必须是完整的、可独立阅读的章节正文，不得有占位符、省略号或"（此处省略）"等
- **字数达标**：章节正文字数应接近目标字数 {{target_words}}

注意：
- 直接输出章节正文，不要包含任何解释性文字
- 不要使用 markdown 代码块标记
- 不要输出 JSON 对象
- 章节正文必须完整，从开头到结尾
- 第一句话就是章节正文的开始，不要有任何前置说明
